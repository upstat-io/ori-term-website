---
import Feature from '../../layouts/Feature.astro';
---

<Feature
  title="MULTI-WINDOW"
  subtitle="Multiple windows, per-window DPI, cross-window tab movement."
>
  <h2>Cross-Window Tab Movement</h2>
  <p>Drag tabs between windows or move a focused tab to a new window with a keyboard shortcut. The shell session, scrollback, and layout travel with the tab — nothing has to be re-opened in the destination window.</p>

  <div class="screenshot-placeholder" role="img" aria-label="Screenshot of multiple ori-term windows with tabs being dragged between them">
    <span>[ SCREENSHOT COMING SOON ]</span>
  </div>

  <h2>Per-Window DPI</h2>
  <p>Each window tracks its own display scale factor. Moving a window from a standard-DPI monitor to a HiDPI display triggers an immediate rescale — fonts, chrome, and terminal content stay sharp without manual intervention.</p>

  <h2>Aero Snap</h2>
  <p>Full Windows Aero Snap support across all windows. Drag to screen edges to snap left, right, or maximize. Each window snaps independently.</p>

  <h2>Clean Shutdown</h2>
  <p>Closing a window cleans up the shells running inside it. Child processes terminate gracefully on every platform — no orphaned shells, no leaked handles, no zombies in your task manager.</p>
</Feature>

<style>
  .roadmap-note {
    border: 1px solid var(--accent);
    background: color-mix(in srgb, var(--accent) 8%, transparent);
    padding: 16px 20px;
    margin-bottom: 32px;
    font-size: 0.85rem;
  }
  .roadmap-note strong {
    color: var(--accent);
    display: block;
    margin-bottom: 6px;
  }
  .screenshot-placeholder {
    border: var(--border-weight) solid var(--border);
    background: var(--bg-raised);
    padding: 120px 24px;
    text-align: center;
    color: var(--text-muted);
    font-size: 0.75rem;
    letter-spacing: 0.2em;
    margin-bottom: 24px;
  }
</style>
